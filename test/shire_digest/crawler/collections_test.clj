(ns shire-digest.crawler.collections-test
  (:require [clojure.test :refer :all]
            [shire-digest.crawler.collections :refer :all]))

(deftest test-crawlers-collection
  (testing "Crawlers collection"
    (is crawlers)))
